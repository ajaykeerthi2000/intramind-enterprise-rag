# Copy this file to .env and fill in the real values. DO NOT commit secrets to source control.
# For local development: cp .env.example .env  (or copy manually on Windows)

# --- Application / runtime ---
APP_ENV=development         # one of: development, staging, production
DEBUG=true                  # true/false
LOG_LEVEL=info              # debug, info, warning, error
HOST=127.0.0.1
PORT=8000

# --- Authentication / Security ---
# Strong random string (keep secret). Use 32+ chars for production.
JWT_SECRET=replace-with-a-strong-random-string
# JWT signing algorithm (default in this project is HS256)
JWT_ALGORITHM=HS256
# Group name required to access the API/application
REQUIRED_GROUP=RAG-App-Users

# --- Third-party APIs ---
# GROQ (used by LangChain/Groq client). Obtain from your provider.
GROQ_API_KEY=your-groq-api-key

# --- Optional / Common services ---
# Local FAISS index or vector store directory (if used by your pipeline)
FAISS_INDEX_PATH=./vector_store/index.faiss
VECTOR_STORE_DIR=./vector_store

# Provider API keys (if you add providers like OpenAI or others)
# OPENAI_API_KEY=your-openai-key

# Notes:
# - Never add real secrets to commits. Use a secret manager or CI environment variables for production.
# - Keep `.env` out of source control; `.env.example` is safe to commit and documents required keys.
